---
name: observability-stop
description: Stop the multi-agent observability server
---

Stop the running observability server.

Execute the following command:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/scripts/stop-server.sh
```

This will gracefully shutdown the server and stop event collection.
