---
name: observability-status
description: Check the status of the observability server
---

Check if the observability server is running and display status information.

Execute the following command:

```bash
bash ${CLAUDE_PLUGIN_ROOT}/scripts/check-status.sh
```

This will show:
- Whether the server is running
- The number of events collected
- The server URL
